package com.github.exformatgames.pacman.managers.net.listeners;

import com.github.exformatgames.pacman.data.EntityData;

public interface EntityCreatedListener {
    void createdEntity(EntityData data);
}
